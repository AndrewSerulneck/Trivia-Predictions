import { RouteLoadingScreen } from "@/components/ui/RouteLoadingScreen";

export default function VenueLoading() {
  return <RouteLoadingScreen title="Opening Venue" subtitle="Loading games and leaderboard..." />;
}
