"use client";

import type { VenueGameKey } from "@/lib/venueGameCards";

export function VenueEntryRulesPanel({
  gameKey: _gameKey,
  shouldDisplay: _shouldDisplay,
}: {
  gameKey: VenueGameKey;
  shouldDisplay: boolean;
}) {
  return null;
}
